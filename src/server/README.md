# Server functions

The single home for TanStack Start server functions (`createServerFn`). Define them here and import into routes/components; keep the actual business logic in `src/services/` and call it from these thin server entrypoints.
