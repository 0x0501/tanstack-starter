# Services

Business / domain logic. Keep it framework-agnostic so routes, server functions, and middleware can all import it. `src/lib/` stays for framework glue and config (auth, email, clients); real domain logic goes here.
